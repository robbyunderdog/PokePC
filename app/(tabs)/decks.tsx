import { Text, View } from "react-native";

export default function DecksScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ fontSize: 24 }}>Your Decks</Text>
    </View>
  );
}